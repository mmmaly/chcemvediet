# vim: expandtab
# -*- coding: utf-8 -*-

from .datacheck import (
        DEBUG, INFO, WARNING, ERROR, CRITICAL,
        Issue, Debug, Info, Warning, Error, Critical,
        registry, register,
        )
